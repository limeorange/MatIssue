export type ExampleItems = {
  title: string;
  description: string;
};

export type User = {
  currentUser: {
    user_id: string;
    username: string;
    email: string;
    birth_date: string;
    img: string;
    created_at: string;
  };
};
